Place production installers in this folder so they can be distributed and downloaded.
Recommended filenames:
- CurrentScope-Setup.exe (Windows)
- CurrentScope.apk (Android)
- CurrentScope.deb (Linux, optional)

The frontend settings page links to /installers/? paths.
If you want those links to work from the dev frontend, copy these files into dashboards/frontend/public/installers as well.
